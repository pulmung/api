// 신고 사유 — 닫힌 집합(enum). 운영 중 배포 없이 늘어나는 사전이 아니라 코드 배포와
// 함께만 변하므로 런타임 조회 API를 만들지 않는다(architecture.md §9 배포 트레인 기준).
//
// wire 값은 영문 소문자 식별자다 — plantCategories(한글)와 다른 선택이고, 이유가 있다:
// 카테고리의 '관엽'은 분류명 자체라 UI 라벨과 값이 같아도 안전하지만, 신고 사유의 한글
// 문구("욕설·혐오 표현")는 **라벨**이라 운영 중 계속 다듬어진다. 라벨을 wire 값으로 쓰면
// 문구 한 번 고칠 때마다 DB 값 마이그레이션이 따라온다. 라벨은 클라가 소유한다.
//
// 두 단어 이상이 필요한 사유는 표기 논쟁(snake_case vs camelCase)을 만들므로 단일어로
// 정리했다 — 아래 주석이 각 값의 실제 범위다(라벨 문구는 클라가 이 범위 안에서 쓴다).
export const reportReasons = [
  // 광고·홍보·도배·반복 게시
  'spam',
  // 욕설·괴롭힘·혐오 표현·비방
  'abuse',
  // 음란물·성적 불쾌감
  'sexual',
  // 폭력·자해·동물학대
  'violence',
  // 불법 거래·규제 위반. 식물 도메인 특화 축이 여기 들어온다 —
  // 멸종위기종(CITES) 거래·불법 채집·검역 미필 반입.
  'illegal',
  // 타인의 개인정보 노출(신상·연락처·위치)
  'privacy',
  // 사진·글 도용
  'copyright',
  // 위 어디에도 안 맞음 — detail(자유 서술)이 사실상 필수가 되는 값.
  // 심사 비용이 가장 비싸므로 목록 맨 아래에 두고 UI도 마지막에 노출한다.
  'other',
] as const;

export type ReportReason = (typeof reportReasons)[number];
